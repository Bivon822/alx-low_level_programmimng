#include "main.h"

/**
 * main - check the code 
 * return: Always 0.
 */
int main(void)
{
	char *str;

	str = "I dont fear computers. I fear the lack of them - Isaack Asimov";
	_puts(str);
	return (0);
}
