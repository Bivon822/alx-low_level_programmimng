#include <stdio.h>
#include "main.h"

/** sum - adds two numbers
 *
 * Return: sum of two numbers
 */
int main(void)
{
        sum();
        printf("hello: ");
        sum();
        printf("Bivon: ");
        sum();
        printf("Iroga: ");
        sum();
        printf("kevo: ");
        sum();
        printf("ochenge: ");
return(0);
}
